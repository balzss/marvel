{
  "name": "react-onboard",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && git push origin `git subtree split --prefix app master`:gh-pages --force"
  },
  "homepage": "https://balzss.github.io/marvel",
  "devDependencies": {}
}
